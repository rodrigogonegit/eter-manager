<Window x:Class="EterManager.UserInterface.Views.UpdateMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EterManager.UserInterface.Views"
        xmlns:converters="clr-namespace:EterManager.UserInterface.Converters"
        mc:Ignorable="d"
        Title="Update Menu - Eter Manager" Height="358.154" Width="622.154">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <TextBox IsReadOnly="True" Margin="10,41,10,39" TextWrapping="Wrap" Text="{Binding Changelog}"/>
        <Label Content="Changelog" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <Button Command="{Binding CheckUpdatesCommand}" Content="Check now" HorizontalAlignment="Left" Margin="10,0,0,10" Width="75" Height="24" VerticalAlignment="Bottom"/>
        <ProgressBar Visibility="{Binding IsUpdating, Converter={StaticResource BoolToVisibilityConverter}}" Value="{Binding DownloadProgress}" HorizontalAlignment="Left" Margin="115,0,0,11" VerticalAlignment="Bottom" Height="19" RenderTransformOrigin="0.5,0.5" Width="97">
        </ProgressBar>

    </Grid>
</Window>
